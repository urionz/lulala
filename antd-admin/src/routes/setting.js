import React, {PropTypes} from 'react';
import {routerRedux} from 'dva/router';
import {connect} from 'dva';

function Setting({location, dispatch}) {

}

export default connect(mapStateToProps)(Setting);
